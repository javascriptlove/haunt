{
    "name": "haunt",
    "version": "1.0.0",
    "scripts": {
        "build": "babel --watch src -d build"
    },
    "devDependencies": {
        "babel-cli": "^6.14.0",
        "babel-preset-es2015": "^6.14.0"
    }
}
